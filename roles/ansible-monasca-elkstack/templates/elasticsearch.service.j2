[Unit]
Description=Elasticsearch
Requires=network.target
After=network.target

[Service]
Type=simple
Restart=on-failure
ExecStart={{ elasticsearch_dest }}/bin/elasticsearch

[Install]
WantedBy=multi-user.target
